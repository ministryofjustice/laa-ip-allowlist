[project]
name = "laa-ip-allowlist"
version = "0.1.0"
description = "Extract CIDRs from a YAML allowlist file by matching tag groups."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
]
